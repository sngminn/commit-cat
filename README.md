# Commit-Cat (cmcat)

The AI-powered smart commit assistant.

## Setup

1. `npm install`
2. Create `.env` with `GEMINI_API_KEY=...`

## Usage

```bash
# Run
node bin/commit-cat.mjs

# Korean Mode
node bin/commit-cat.mjs -k
```
